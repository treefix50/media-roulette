version: '3.8'

services:
  media-roulette:
    build: .
    container_name: media-roulette
    ports:
      - "8000:8000"
    environment:
      MOVIES_DIR: /data/movies
      SERIES_DIR: /data/series
      DATABASE_PATH: /state/media-roulette.db
    volumes:
      - /mnt/data:/data:ro
      - ./state:/state
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  default:
    name: media-roulette-network
